{
  "name": "huggingface-models-assets-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/worker.js",
    "types": "wrangler types worker-configuration.d.ts",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "startup": "wrangler check startup"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "wrangler": "^4.123.0"
  },
  "allowScripts": {
    "esbuild": true,
    "workerd": true
  }
}
